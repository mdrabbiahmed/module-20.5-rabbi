function swap(a,b){

            // let t = a;
            // a=b;
            // b=t;

            // a= a+b;
            // b = a-b;
            // a = a-b;
            // return {a,b};
            
            // [a,b]=[b,a];

      // return {a,b};
}

let result = swap(50,20);
console.log(result);